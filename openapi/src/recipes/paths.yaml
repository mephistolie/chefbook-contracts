paths:
  /v1/recipes:
    get:
      description: Get recipes by query
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: schemas.yaml#/schemas/RecipesResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Get recipes
      tags:
        - recipe
      x-codegen-request-body-name: input
      operationId: getRecipes
      parameters:
        - in: query
          name: author_id
          required: false
          schema:
            type: string
        - in: query
          name: count
          required: false
          schema:
            type: integer
        - in: query
          name: last_calories
          required: false
          schema:
            type: integer
        - in: query
          name: last_creation_timestamp
          required: false
          schema:
            type: string
        - in: query
          name: last_rating
          required: false
          schema:
            type: number
        - in: query
          name: last_recipe_id
          required: false
          schema:
            type: string
        - in: query
          name: last_time
          required: false
          schema:
            type: integer
        - in: query
          name: last_update_timestamp
          required: false
          schema:
            type: string
        - in: query
          name: last_votes
          required: false
          schema:
            type: integer
        - in: query
          name: max_calories
          required: false
          schema:
            type: integer
        - in: query
          name: max_rating
          required: false
          schema:
            type: integer
        - in: query
          name: max_servings
          required: false
          schema:
            type: integer
        - in: query
          name: max_time
          required: false
          schema:
            type: integer
        - in: query
          name: min_calories
          required: false
          schema:
            type: integer
        - in: query
          name: min_rating
          required: false
          schema:
            type: integer
        - in: query
          name: min_servings
          required: false
          schema:
            type: integer
        - in: query
          name: min_time
          required: false
          schema:
            type: integer
        - in: query
          name: owned
          required: false
          schema:
            type: boolean
        - in: query
          name: recipe_id
          required: false
          schema:
            items:
              type: string
            type: array
          style: form
          explode: true
        - in: query
          name: recipe_language
          required: false
          schema:
            items:
              type: string
            type: array
          style: form
          explode: true
        - in: query
          name: saved
          required: false
          schema:
            type: boolean
        - in: query
          name: search
          required: false
          schema:
            type: string
        - in: query
          name: sorting
          required: false
          schema:
            type: string
        - in: query
          name: tags
          required: false
          schema:
            items:
              type: string
            type: array
          style: form
          explode: true
        - in: query
          name: user_language
          required: false
          schema:
            type: string

    post:
      description: Create recipe
      requestBody:
        content:
          application/json:
            schema:
              $ref: schemas.yaml#/schemas/SaveRecipeRequest
            examples:
              minimal:
                summary: Minimal recipe
                description: The ingredient and cooking arrays are present even when
                  empty.
                value:
                  name: Tomato soup
                  ingredients: []
                  cooking: []
              complete:
                summary: Recipe with ingredients and cooking steps
                description: Use real item UUIDs and the current version when updating
                  an existing recipe.
                value:
                  name: Tomato soup
                  visibility: public
                  encrypted: false
                  language: en
                  description: A simple tomato soup.
                  servings: 2
                  time: 20
                  calories: 180
                  macronutrients:
                    protein: 5
                    fats: 7
                    carbohydrates: 24
                  ingredients:
                    - id: 11111111-1111-4111-8111-111111111111
                      type: ingredient
                      text: Tomatoes
                      amount: 400
                      unit: g
                  cooking:
                    - id: 22222222-2222-4222-8222-222222222222
                      type: step
                      text: Simmer gently.
                      time: 600
                  id: 33333333-3333-4333-8333-333333333333
        description: Input
        required: true
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: schemas.yaml#/schemas/RecipeCreatedResponse
          description: Created
          headers:
            Location:
              description: URI of the created resource
              schema:
                type: string
                format: uri-reference

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Create recipe
      tags:
        - recipe
      x-codegen-request-body-name: input
      operationId: createRecipe

  /v1/recipes/book:
    get:
      description: Get user recipe book
      parameters:
        - name: language
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: schemas.yaml#/schemas/RecipeBookResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Get recipe book
      tags:
        - recipe
      operationId: getRecipeBook

  /v1/recipes/random:
    get:
      description: Get random recipe
      parameters:
        - name: language
          in: query
          required: false
          style: form
          explode: true
          schema:
            type: array
            items:
              type: string
          description: Repeated language values; the first value is also used as the
            user language by the current gateway.
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: schemas.yaml#/schemas/RecipeResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Get random recipe
      tags:
        - recipe
      operationId: getRandomRecipe

  /v1/recipes/{recipe_id}:
    get:
      description: Get recipe
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
        - name: language
          in: query
          required: false
          schema:
            type: string
        - name: translated
          in: query
          required: false
          schema:
            type: boolean
        - name: translator_id
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: schemas.yaml#/schemas/RecipeResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '404':
          description: Referenced resource not found
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Get recipe
      tags:
        - recipe
      operationId: getRecipe

    put:
      description: Update recipe
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: schemas.yaml#/schemas/SaveRecipeRequest
            examples:
              minimal:
                summary: Minimal recipe
                description: The ingredient and cooking arrays are present even when
                  empty.
                value:
                  name: Tomato soup
                  ingredients: []
                  cooking: []
              complete:
                summary: Recipe with ingredients and cooking steps
                description: Use real item UUIDs and the current version when updating
                  an existing recipe.
                value:
                  name: Tomato soup
                  visibility: public
                  encrypted: false
                  language: en
                  description: A simple tomato soup.
                  servings: 2
                  time: 20
                  calories: 180
                  macronutrients:
                    protein: 5
                    fats: 7
                    carbohydrates: 24
                  ingredients:
                    - id: 11111111-1111-4111-8111-111111111111
                      type: ingredient
                      text: Tomatoes
                      amount: 400
                      unit: g
                  cooking:
                    - id: 22222222-2222-4222-8222-222222222222
                      type: step
                      text: Simmer gently.
                      time: 600
                  version: 3
        description: Input
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: schemas.yaml#/schemas/RecipeVersionResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '404':
          description: Referenced resource not found
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Update recipe
      tags:
        - recipe
      x-codegen-request-body-name: input
      operationId: updateRecipe

    delete:
      description: Delete recipe
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/MessageResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '404':
          description: Referenced resource not found
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Delete recipe
      tags:
        - recipe
      operationId: deleteRecipe

  /v1/recipes/{recipe_id}/book:
    post:
      description: Save recipe to user's recipe book
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/MessageResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Save recipe
      tags:
        - recipe
      operationId: saveRecipeToRecipeBook

    delete:
      description: Remove recipe from recipe book
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/MessageResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Remove recipe from recipe book
      tags:
        - recipe
      operationId: removeRecipeFromRecipeBook

  /v1/recipes/{recipe_id}/collections:
    put:
      description: Set recipe collections
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/MessageResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Set recipe collections
      tags:
        - recipe
      operationId: setRecipeCollections
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: schemas.yaml#/schemas/SetRecipeCollectionsRequest
            examples:
              assign:
                summary: Put a recipe in collections
                value:
                  collections:
                    - 11111111-1111-4111-8111-111111111111
                    - 22222222-2222-4222-8222-222222222222
              clear:
                summary: Remove collection assignments
                value:
                  collections: []

  /v1/recipes/{recipe_id}/collections/{collection_id}:
    post:
      description: Add recipe to collection
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
        - description: Collection ID
          in: path
          name: collection_id
          required: true
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/MessageResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Add recipe to collection
      tags:
        - recipe
      operationId: addRecipeToCollection

    delete:
      description: Remove recipe from collection
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
        - description: Collection ID
          in: path
          name: collection_id
          required: true
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/MessageResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Remove recipe from collection
      tags:
        - recipe
      operationId: removeRecipeFromCollection

  /v1/recipes/{recipe_id}/favourites:
    post:
      description: Add recipe to favourite
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/MessageResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Add recipe to favourite
      tags:
        - recipe
      operationId: saveRecipeToFavourites

    delete:
      description: Remove recipe from favourite
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/MessageResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Remove recipe from favourite
      tags:
        - recipe
      operationId: removeRecipeFromFavourites

  /v1/recipes/{recipe_id}/pictures:
    post:
      description: Generate recipe pictures upload links
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: schemas.yaml#/schemas/CreateRecipePictureUploadLinksRequest
            examples:
              count1:
                summary: Upload 1 picture(s)
                description: Upload bytes to the returned links, then submit the resulting
                  picture references.
                value:
                  picturesCount: 1
              count3:
                summary: Upload 3 picture(s)
                description: Upload bytes to the returned links, then submit the resulting
                  picture references.
                value:
                  picturesCount: 3
        description: Input
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: schemas.yaml#/schemas/RecipePictureUploadLinksResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Generate recipe pictures upload links
      tags:
        - recipe
      x-codegen-request-body-name: input
      operationId: generateRecipePicturesUploadLinks

    put:
      description: Set recipe pictures
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: schemas.yaml#/schemas/SetRecipePicturesRequest
            examples:
              previewAndStep:
                summary: Set preview and step pictures
                description: Replace sample picture references with those returned
                  by the upload flow.
                value:
                  pictures:
                    preview: example-preview-object
                    cooking:
                      22222222-2222-4222-8222-222222222222:
                        - example-step-object
                  version: 3
              clear:
                summary: Remove recipe pictures
                value:
                  pictures:
                    preview: null
                    cooking: {}
                  version: 3
        description: Pictures
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: schemas.yaml#/schemas/RecipePicturesResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '404':
          description: Referenced resource not found
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Set recipe pictures
      tags:
        - recipe
      x-codegen-request-body-name: input
      operationId: setRecipePictures

  /v1/recipes/{recipe_id}/rate:
    post:
      description: Rate recipe
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/MessageResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Rate recipe
      tags:
        - recipe
      operationId: rateRecipe
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: schemas.yaml#/schemas/RateRecipeRequest
            examples:
              fiveStars:
                summary: Rate a recipe
                value:
                  score: 5
              removeRating:
                summary: Remove my rating
                value:
                  score: null

  /v1/recipes/{recipe_id}/translations:
    post:
      description: Translate recipe
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: schemas.yaml#/schemas/TranslateRecipeRequest
            examples:
              english:
                summary: Translate into English
                description: Keys refer to the original ingredient/step IDs.
                value:
                  language: en
                  name: Tomato soup
                  description: A warming soup.
                  ingredients:
                    11111111-1111-4111-8111-111111111111:
                      text: Tomatoes
                      unit: g
                  cooking:
                    22222222-2222-4222-8222-222222222222: Simmer gently.
              russian:
                summary: Translate into Russian
                value:
                  language: ru
                  name: Томатный суп
                  ingredients:
                    11111111-1111-4111-8111-111111111111:
                      text: Помидоры
                      unit: г
                  cooking:
                    22222222-2222-4222-8222-222222222222: Томите на медленном огне.
        description: Input
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/MessageResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Translate recipe
      tags:
        - recipe
      x-codegen-request-body-name: input
      operationId: translateRecipe

  /v1/recipes/{recipe_id}/translations/{language_code}:
    delete:
      description: Delete recipe translation
      parameters:
        - description: Recipe ID
          in: path
          name: recipe_id
          required: true
          schema:
            type: string
        - description: Recipe ID
          in: path
          name: language_code
          required: true
          schema:
            type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/MessageResponse
          description: OK

        '400':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Bad Request

        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '403':
          description: Forbidden An authenticated session with emailVerificationRequired
            cannot use this operation; return email_verification_required. Enforce
            the current account state on the server, independently of the token response
            snapshot.
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
              examples:
                emailVerificationRequired:
                  value:
                    error: email_verification_required
                    message: Verify your email to perform this operation.

        '409':
          description: Conflict with current resource state, version, or account deletion
            state
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse

        '500':
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
          description: Internal Server Error

        '503':
          description: Upstream service unavailable; retry later
          content:
            application/json:
              schema:
                $ref: ../common/schemas.yaml#/schemas/ErrorResponse
      security:
        - bearerAuth: []
      summary: Delete recipe translation
      tags:
        - recipe
      operationId: deleteRecipeTranslation
